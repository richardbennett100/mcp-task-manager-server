# .env.test
PGPASSWORD=mysecretpassword
# Add other PG variables if needed (PGHOST, PGUSER, etc.) if they differ for tests
# PGHOST=localhost
# PGPORT=5432
# PGUSER=taskmanager_user
# PGDATABASE=taskmanager_db_test # Consider a separate test DB?